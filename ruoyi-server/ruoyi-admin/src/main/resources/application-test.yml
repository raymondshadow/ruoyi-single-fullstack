# 测试环境覆盖配置
ruoyi:
  profile: ./uploadPath-test

spring:
  datasource:
    druid:
      master:
        url: jdbc:mysql://localhost:3306/ry-vue-test?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
        username: root
        password: password
      slave:
        username: root
        password: password
  data:
    redis:
      host: localhost
      port: 6379
      database: 1
      password: 123456

logging:
  level:
    com.ruoyi: info
